# BuildTools working dir + jar output (local builds)
buildtools
dist

# Nix build symlinks
result
result-*

# node (matrix.ts runs via npx tsx)
node_modules

# check-outdated local run artifacts (StopOnStart plugin build + server junk)
plugin-build
StopOnStart.jar
plugins
eula.txt
bukkit.yml
*.log

# Claude Code entrypoint — just points at AGENTS.md (the tracked source of truth)
CLAUDE.md
